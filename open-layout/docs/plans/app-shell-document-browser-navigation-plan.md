# App Shell Document Browser Navigation Plan

## Goal
Add navigation from all applications using `scribus-app-shell` back to the document browser.

## Steps
- [x] Identify all app-shell-based applications and shared shell integration points.
- [ ] Implement a shared shell-level navigation control to `/document-browser/`.
- [ ] Add/adjust Playwright coverage to verify the navigation is present and functional.
- [ ] Run required verification (`npm test` then `npm run test:e2e`) and inspect browser console logs.
